#include <iostream>
#include "Heap.h"
#include <vector>
using namespace std;

vector& getLeastNumbers(vector arr, int k)
{
    if(k <= 0 || arr.empty() ){
        return [];
    }
}

int main()
{

    return 0;
}